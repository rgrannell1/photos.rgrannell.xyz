/* CSS selectors used for DOM queries. */

// Album year headings used for scroll tracking.
export const ALBUM_YEAR_HEADINGS_SELECTOR = ".album-container .year-heading";

// Prefix shared by album year heading IDs and year-scroll lookups.
export const ALBUM_YEAR_HEADING_ID_PREFIX = "year-";

// Image used for the album banner parallax effect.
export const ALBUM_BANNER_IMAGE_SELECTOR = ".album-banner-image";

// Class applied to banners with CSS parallax support.
export const PARALLAX_CSS_CLASS = "parallax-css";

// Class applied to each heart-rain element.
export const HEART_RAIN_HEART_CLASS = "heart-rain-heart";

// Container for the heart-rain overlay.
export const HEART_RAIN_OVERLAY_SELECTOR = ".heart-rain-overlay";

// Leaflet map container within the map page.
export const LEAFLET_MAP_SELECTOR = ".leaflet-map";

// Placeholder hidden after its full image loads.
export const THUMBNAIL_PLACEHOLDER_SELECTOR = ".thumbnail-placeholder";
